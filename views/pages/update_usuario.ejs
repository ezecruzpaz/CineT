<!DOCTYPE html>
<html lang="es">

<head>
    <!--fuente-->
    
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css" />
    <link rel="stylesheet" href="../public/css/bootstrap.min.css" />
    <link rel="stylesheet" href="../public/css/menu.css" />
    <link rel="stylesheet" href="../public/css/home.css" />
    <title>Actualizar Usuario</title>
    <!-- Incluir el archivo head.ejs -->
    <%- include('../includes/head') %>

</head>


<body>
    <header class="header">
        
        </a>
        <input class="menu-btn" type="checkbox" id="menu-btn" />
        <label class="menu-icon" for="menu-btn"><span class="navicon"></span></label>
        <ul class="menu">
            <li>
                <a class="menu-item" href="/">Inicio</a>
            </li>
            <li>
                <a class="menu-item" href="/Crud-Completo-con-NodeJS-Express-y-MySQL">Crud</a>
            </li>
        </ul>
    </header>

    <div class="container" style="margin-top: 80px !important">
        <div class="row justify-content-md-center mb-5">
            <div class="col-md-6 mb-5">
                <h3 class="text-center">
                   
                    Actualizar Usuario
                </h3>
                <form action="/actualizar-usuario/<%= usuario.id_usuario %>" method="POST">
                    <input type="hidden" name="_method" value="PUT">
                    <div class="mb-3">
                        <label for="nombre_usuario" class="form-label"> Nombre de Usuario </label>
                        <input
                            type="text"
                            name="nombre_usuario"
                            class="form-control"
                            value="<%= usuario.nombre_usuario %>"
                            readonly
                            required
                        />
                    </div>
                    <div class="mb-3">
                        <label for="contrasena_encriptada" class="form-label"> Contraseña </label>
                        <input
                            type="password"
                            name="contrasena_encriptada"
                            class="form-control"
                            placeholder="Nueva contraseña"
                            minlength="6"
                            maxlength="20"
                        />
                        <small id="passwordHelp" class="form-text text-muted">
                            <!--La contraseña debe tener al menos 6 caracteres y como máximo 20 caracteres.-->
                        </small>
                    </div>
                    <div class="mb-3">
                        <label for="tipo_usuario">Tipo de Usuario</label>
                        <select name="tipo_usuario" class="form-select" required>
                            <option value="">Seleccionar tipo de usuario</option>
                            <option value="super admin" <% if (usuario.tipo_usuario === "super admin") { %>selected<% } %>>Super Admin</option>
                            <option value="taquillero" <% if (usuario.tipo_usuario === "taquillero") { %>selected<% } %>>Taquillero</option>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label for="estado">Estado</label>
                        <select name="estado" class="form-select" required>
                            <option value="">Seleccionar estado</option>
                            <option value="activo" <% if (usuario.estado === "activo") { %>selected<% } %>>Activo</option>
                            <option value="inactivo" <% if (usuario.estado === "inactivo") { %>selected<% } %>>Inactivo</option>
                        </select>
                    </div>

                    <div class="d-grid gap-2 col-12 mx-auto mt-4 mb-4">
                        <button class="btn btn-primary" type="submit">
                            Actualizar Usuario
                        </button>
                    </div>

                   
                    <div href="/Crud-Completo-con-NodeJS-Express-y-MySQL"class="d-grid gap-2 col-12 mx-auto mt-4 mb-4" >
                        <button class="btn btn-primary" type="submit">
                            Cancelar
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>


    <%- include('../includes/scripts') %>
</body>

</html>
<style>
    form {
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.1);
      }
     
</style>
